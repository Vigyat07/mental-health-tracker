import React from "react";
import { Typography } from "@mui/material";


const Header = () => (
  <header>
    <Typography className="header-title">AI Mental Health Tracker</Typography>
  </header>
);

export default Header;
